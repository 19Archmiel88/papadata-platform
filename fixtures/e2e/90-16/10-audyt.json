{
  "fixtureId": "E2E_90_16_10_audyt",
  "flowId": "90.16",
  "stepNo": 10,
  "stepName": "Audyt",
  "surfaceDocument": "16-ustawienia-zespol-bezpieczenstwo/60-07-audyt.md",
  "route": "/app/settings/audyt",
  "operationId": "settings.audit.read",
  "operationKind": "query",
  "capability": "settings.read",
  "apiRequestFixture": "fixtures/api/settings.audit.read.request.json",
  "apiResponseFixture": "fixtures/api/settings.audit.read.response.json",
  "actor": {
    "role": "Admin",
    "tenantId": "tenant-e2e",
    "workspaceId": "workspace-e2e",
    "capabilities": [
      "settings.read"
    ]
  },
  "preconditions": [
    "Warunek domenowy ai action approval audyt: Aktor posiada `settings.read`; fixture ładuje request `fixtures/api/settings.audit.read.request.json` i stan domeny wymagany dla kroku 10 procesu 90.16. Poprzedni krok zakończył się potwierdzonym postcondition albo jest to punkt startowy."
  ],
  "expected": {
    "success": "Odpowiedź `SettingsAuditReadResponse` ma operationId `settings.audit.read`, brak ApiProblem krytycznego, a UI renderuje dane kroku „Audyt” bez zamiany null na zero.",
    "postcondition": "Po kroku „Audyt” obowiązuje domenowy warunek: operacja `settings.audit.read` jest widoczna w audycie, fixture response `fixtures/api/settings.audit.read.response.json` przechodzi walidację schema, a następna powierzchnia „Outcome” jest osiągalna wyłącznie zgodnie z procesem 90.16.",
    "assertion": "Asercja domenowa ai action approval audyt: Sprawdź operation kind `query`, capability `settings.read`, schema request/response, expected event `e2e_90_16_10_settings_audit_read`, brak podwójnego efektu, prawidłowy focus po zmianie powierzchni oraz kod `SUBSCRIPTION_ACTIVATION_FAILED` w kontrolowanym scenariuszu błędu."
  },
  "recoverableError": {
    "code": "SUBSCRIPTION_ACTIVATION_FAILED",
    "behavior": "Odzyskanie kroku ai action approval audyt: `SUBSCRIPTION_ACTIVATION_FAILED` dla kroku 10: zachowaj niesekretne dane formularza i route `/app/settings/audyt`, pokaż correlationId oraz akcję retry tylko dla błędu oznaczonego `recoverable=true`; błąd capability kończy proces bez retry.",
    "resume": "Reguła wznowienia ai action approval audyt: Checkpoint `E2E_90_16_10_audyt` zapisuje zakończony step_no=10, operationId i identyfikatory zasobów z response fixture. Po wznowieniu system ponownie sprawdza `settings.read` i odczytuje aktualny stan; komendy nie są automatycznie odtwarzane bez tego samego idempotency key."
  }
}
