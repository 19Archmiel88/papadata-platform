// Shared test doubles for the backend test suites (apps/api, apps/bff,
// packages/database, packages/contracts). Fleshed out alongside the Vitest
// rollout: RequestPrincipal builders, a fake ProductionDatabase/withTenantWorkspace
// harness, and HMAC/JWT test-signing helpers matching PrincipalService's scheme.

export const papadataTestingPackageVersion = "0.1.0";
