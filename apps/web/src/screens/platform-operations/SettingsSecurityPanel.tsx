import { useState } from 'react';
import { Button, ProductSectionFrame } from '../../design-system';
import type { BffSessionSummary } from '../../runtime/shared/api/bffClient';
import { useProductLocale } from '../shared/useProductLocale';
import { OperationError, OperationsDialog } from './OperationsFrame';
export function SettingsSecurityPanel({sessions,mfaEnabled,onRevoke,onConfigureMfa,onDisableMfa}:{sessions:readonly BffSessionSummary[];mfaEnabled:boolean;onRevoke?:(id:string)=>Promise<void>;onConfigureMfa?:()=>void;onDisableMfa?:()=>Promise<void>}){
 const {t}=useProductLocale(),[disable,setDisable]=useState(false),[target,setTarget]=useState<BffSessionSummary|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState<string|null>(null);
 return <ProductSectionFrame icon="security" title={t('Bezpieczenstwo konta','Account security')} description={t('Sesje dotycza Twojego konta, nie wszystkich czlonkow organizacji.','Sessions belong to your account, not to every member of the organization.')}>
 <p>MFA: {mfaEnabled?t('wlaczone','enabled'):t('wylaczone','disabled')}</p>{onConfigureMfa&&<Button variant="secondary" onClick={onConfigureMfa}>{t('Otworz konfiguracje MFA','Open MFA setup')}</Button>}
 {mfaEnabled&&onDisableMfa&&<Button variant="secondary" onClick={()=>{setDisable(true);setError(null);}}>{t('Wylacz MFA','Disable MFA')}</Button>}
 <p>{t('Kody odzyskiwania sa jednorazowe i pokazywane tylko podczas konfiguracji. Ten widok nie odtwarza utraconych kodow ani nie omija potwierdzenia tozsamosci.','Recovery codes are single-use and shown only during enrollment. This screen cannot recover lost codes or bypass identity verification.')}</p>
 <div className="pd-operations__table"><table><caption>{t('Aktywne sesje','Active sessions')}</caption><thead><tr><th>{t('Sesja','Session')}</th><th>{t('Urzadzenie','Device')}</th><th>{t('Utworzona','Issued')}</th><th>{t('Wazna do','Expires')}</th><th>{t('Akcja','Action')}</th></tr></thead><tbody>{sessions.map(session=><tr key={session.sessionId}><td>{session.current?t('Biezaca sesja','Current session'):session.sessionId.slice(0,8)}</td><td>{session.userAgent??t('Brak informacji','Not available')}</td><td>{session.issuedAt}</td><td>{session.expiresAt}</td><td>{onRevoke&&<Button size="small" variant="ghost" onClick={()=>{setTarget(session);setError(null);}}>{t('Uniewaznij','Revoke')}</Button>}</td></tr>)}</tbody></table></div>
 <OperationsDialog open={target!==null} title={t('Uniewaznic sesje?','Revoke session?')} busy={busy} onClose={()=>setTarget(null)}><p>{target?.current?t('Ta operacja wyloguje Cie z biezacej sesji.','This will sign you out of the current session.'):t('Wybrane urzadzenie utraci dostep.','The selected device will lose access.')}</p><OperationError message={error}/><Button disabled={busy} onClick={()=>{if(!target||!onRevoke)return;setBusy(true);void onRevoke(target.sessionId).then(()=>setTarget(null)).catch(cause=>setError(cause instanceof Error?cause.message:'Operation failed')).finally(()=>setBusy(false));}}>{t('Potwierdz','Confirm')}</Button></OperationsDialog>
 <OperationsDialog open={disable} title={t('Wylaczyc MFA i zakonczyc sesje?','Disable MFA and end sessions?')} busy={busy} onClose={()=>setDisable(false)}><p>{t('Wymagane jest ponowne uwierzytelnienie. Po potwierdzeniu wszystkie sesje konta zostana uniewaznione, takze ta sesja.','Reauthentication is required. Confirmation revokes every account session, including this one.')}</p><OperationError message={error}/><Button disabled={busy} onClick={()=>{if(!onDisableMfa)return;setBusy(true);setError(null);void onDisableMfa().then(()=>setDisable(false)).catch(cause=>setError(cause instanceof Error?cause.message:'MFA change failed')).finally(()=>setBusy(false));}}>{t('Potwierdz wylaczenie','Confirm disable')}</Button></OperationsDialog>
 </ProductSectionFrame>;
}
