import type { AiEvaluationMode, AiEvaluationResult, AiModelRoute } from "@papadata/contracts";
export type AiMessage={readonly role:"system"|"user"|"assistant"|"tool";readonly content:string};export type AiProviderRequest={readonly modelId:string;readonly messages:readonly AiMessage[];readonly maxOutputTokens:number;readonly temperature:number};export type AiProviderResponse={readonly output:string;readonly inputTokens:number;readonly outputTokens:number;readonly providerRequestId:string|null};
export interface AiProviderAdapter{readonly providerId:string;generate(request:AiProviderRequest,signal?:AbortSignal):Promise<AiProviderResponse>}
export class OpenAiCompatibleProvider implements AiProviderAdapter{readonly providerId:string;private readonly endpoint:string;private readonly apiKey:string;constructor(input:{providerId:string;endpoint:string;apiKey:string}){this.providerId=input.providerId;this.endpoint=input.endpoint.replace(/\/$/,"");this.apiKey=input.apiKey;}async generate(request:AiProviderRequest,signal?:AbortSignal):Promise<AiProviderResponse>{const response=await fetch(`${this.endpoint}/chat/completions`,{method:"POST",headers:{authorization:`Bearer ${this.apiKey}`,"content-type":"application/json"},body:JSON.stringify({model:request.modelId,messages:request.messages,max_tokens:request.maxOutputTokens,temperature:request.temperature}),signal});if(!response.ok)throw new Error(`AI provider error: ${response.status}`);const body=await response.json() as {id?:string;choices?:Array<{message?:{content?:string}}>;usage?:{prompt_tokens?:number;completion_tokens?:number}};return{output:body.choices?.[0]?.message?.content??"",inputTokens:body.usage?.prompt_tokens??0,outputTokens:body.usage?.completion_tokens??0,providerRequestId:body.id??null};}}
export class AiModelRouter{private readonly routes:readonly AiModelRoute[];private readonly providers:ReadonlyMap<string,AiProviderAdapter>;constructor(routes:readonly AiModelRoute[],providers:readonly AiProviderAdapter[]){this.routes=routes;this.providers=new Map(providers.map(p=>[p.providerId,p]));}resolve(useCase:string,dataClasses:readonly string[]):{route:AiModelRoute;provider:AiProviderAdapter}{const route=this.routes.find(c=>c.enabled&&c.useCase===useCase&&dataClasses.every(v=>c.dataClasses.includes(v)));if(!route)throw new Error("No allowed AI model route");const provider=this.providers.get(route.providerId);if(!provider)throw new Error("AI provider is not configured");return{route,provider};}}
export class AiBudgetGuard{assertWithinBudget(input:{estimatedCostMinor:number;route:AiModelRoute;consumedCostMinor:number;workspaceBudgetMinor:number}):void{if(input.estimatedCostMinor>input.route.maxCostMinor)throw new Error("AI route cost limit exceeded");if(input.consumedCostMinor+input.estimatedCostMinor>input.workspaceBudgetMinor)throw new Error("AI workspace budget exceeded");}}
export type EvaluationCase={readonly caseId:string;readonly execute:()=>Promise<boolean>;readonly evidenceReference:string};
export async function runEvaluationSuite(input:{runId:string;mode:AiEvaluationMode;providerId:string|null;modelId:string|null;cases:readonly EvaluationCase[]}):Promise<AiEvaluationResult>{let passed=0;const evidenceReferences:string[]=[];for(const testCase of input.cases){if(await testCase.execute())passed+=1;evidenceReferences.push(testCase.evidenceReference);}return{runId:input.runId,mode:input.mode,providerId:input.providerId,modelId:input.modelId,casesExecuted:input.cases.length,casesPassed:passed,metrics:{passRate:input.cases.length===0?0:passed/input.cases.length},evidenceReferences,createdAt:new Date().toISOString()};}
