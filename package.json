{
  "name": "papadata-platform",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @papadata/web dev",
    "storybook": "pnpm --filter @papadata/web storybook",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test --concurrency=1",
    "test:e2e:auth": "pnpm --filter @papadata/web test:e2e:auth",
    "test:storybook": "pnpm --filter @papadata/web test:storybook",
    "build": "turbo run build",
    "build:storybook": "pnpm --filter @papadata/web build:storybook",
    "preview": "pnpm --filter @papadata/web preview",
    "verify": "pnpm lint && pnpm typecheck && pnpm spell && pnpm markdownlint && pnpm test",
    "build-storybook": "pnpm build:storybook",
    "spell": "cspell \"**/*.{ts,tsx,md,css}\"",
    "markdownlint": "markdownlint-cli2",
    "start:local": "docker compose --env-file .env.example up --build api worker",
    "migrate": "docker compose --env-file .env.example run --rm migrate up",
    "migrate:status": "docker compose --env-file .env.example run --rm migrate status",
    "test:migrations": "docker compose --env-file .env.example run --rm migrate test",
    "security:audit": "pnpm audit --prod",
    "security:check": "pnpm audit --prod && pnpm build && pnpm build-storybook"
  },
  "devDependencies": {
    "@cspell/dict-pl_pl": "^3.0.6",
    "cspell": "^10.0.1",
    "cspell-cli": "^10.0.1",
    "markdownlint-cli2": "^0.23.1",
    "turbo": "2.10.5",
    "typescript": "~6.0.3"
  },
  "packageManager": "pnpm@10.29.3",
  "engines": {
    "node": "24.18.0",
    "pnpm": "10.29.3"
  }
}
