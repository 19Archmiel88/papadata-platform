// Generated by tools/generate-backend-contract-runtime.mjs. Do not edit manually.
import { Controller, Get, Headers, Inject, Post, Put, Req } from "@nestjs/common";
import type { FastifyRequest } from "fastify";
import { AuditDeniedAccess, OperationId, RequireAuthLevel, RequireCapabilities } from "../../auth/route-policy.js";
import { Principal } from "../../auth/principal.decorator.js";
import type { RequestPrincipal } from "../../auth/request-principal.js";
import { ContractRuntimeService } from "../contract-runtime.service.js";
import { contractRequest } from "../contract-runtime.request.js";

@Controller()
export class BillingContractController {
  constructor(
    @Inject(ContractRuntimeService) private readonly runtime: ContractRuntimeService,
  ) {}

  @Get("v1/billing/korekty")
  @OperationId("billing.adjustments.read")
  @RequireCapabilities("billing.read")
  billing_adjustments_read_1(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("billing.adjustments.read", "GET", "/v1/billing/korekty", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/billing/zmiana-i-anulowanie")
  @OperationId("billing.change-cancel.read")
  @RequireCapabilities("billing.read")
  billing_change_cancel_read_2(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("billing.change-cancel.read", "GET", "/v1/billing/zmiana-i-anulowanie", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/billing/entitlements")
  @OperationId("billing.entitlements.read")
  @RequireCapabilities("billing.read")
  billing_entitlements_read_3(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("billing.entitlements.read", "GET", "/v1/billing/entitlements", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/billing/faktury")
  @OperationId("billing.invoices.read")
  @RequireCapabilities("billing.read")
  billing_invoices_read_4(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("billing.invoices.read", "GET", "/v1/billing/faktury", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/billing/zalegla-platnosc")
  @OperationId("billing.overdue-payment.read")
  @RequireCapabilities("billing.read")
  billing_overdue_payment_read_5(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("billing.overdue-payment.read", "GET", "/v1/billing/zalegla-platnosc", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/billing/overdue/resolve")
  @OperationId("billing.overdue.resolve")
  @RequireCapabilities("billing.manage")
  @RequireAuthLevel("step_up")
  @AuditDeniedAccess()
  billing_overdue_resolve_6(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("billing.overdue.resolve", "POST", "/v1/billing/overdue/resolve", request, correlationId, idempotencyKey),
    );
  }


  @Put("v1/billing/payment-method")
  @OperationId("billing.payment.method.update")
  @RequireCapabilities("billing.manage")
  @RequireAuthLevel("step_up")
  @AuditDeniedAccess()
  billing_payment_method_update_7(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("billing.payment.method.update", "PUT", "/v1/billing/payment-method", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/billing/platnosci")
  @OperationId("billing.payments.read")
  @RequireCapabilities("billing.read")
  billing_payments_read_8(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("billing.payments.read", "GET", "/v1/billing/platnosci", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/billing/pilot-do-abonamentu")
  @OperationId("billing.pilot-to-subscription.read")
  @RequireCapabilities("billing.read")
  billing_pilot_to_subscription_read_9(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("billing.pilot-to-subscription.read", "GET", "/v1/billing/pilot-do-abonamentu", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/billing/pilot")
  @OperationId("billing.pilot.read")
  @RequireCapabilities("billing.read")
  billing_pilot_read_10(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("billing.pilot.read", "GET", "/v1/billing/pilot", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/billing/plan/select")
  @OperationId("billing.plan.select")
  @RequireCapabilities("billing.manage")
  @RequireAuthLevel("step_up")
  @AuditDeniedAccess()
  billing_plan_select_11(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("billing.plan.select", "POST", "/v1/billing/plan/select", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/billing/plany")
  @OperationId("billing.plans.read")
  @RequireCapabilities("billing.read")
  billing_plans_read_12(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("billing.plans.read", "GET", "/v1/billing/plany", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/billing/read")
  @OperationId("billing.read")
  @RequireCapabilities("billing.read")
  billing_read_13(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("billing.read", "GET", "/v1/billing/read", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/billing/subscription/activate")
  @OperationId("billing.subscription.activate")
  @RequireCapabilities("billing.manage")
  @RequireAuthLevel("step_up")
  @AuditDeniedAccess()
  billing_subscription_activate_14(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("billing.subscription.activate", "POST", "/v1/billing/subscription/activate", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/billing/subskrypcja")
  @OperationId("billing.subscription.read")
  @RequireCapabilities("billing.read")
  billing_subscription_read_15(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("billing.subscription.read", "GET", "/v1/billing/subskrypcja", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/billing/uzycie-i-limity")
  @OperationId("billing.usage-limits.read")
  @RequireCapabilities("billing.read")
  billing_usage_limits_read_16(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("billing.usage-limits.read", "GET", "/v1/billing/uzycie-i-limity", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/billing/write")
  @OperationId("billing.write")
  @RequireCapabilities("billing.manage")
  @RequireAuthLevel("step_up")
  @AuditDeniedAccess()
  billing_write_17(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("billing.write", "POST", "/v1/billing/write", request, correlationId, idempotencyKey),
    );
  }
}
