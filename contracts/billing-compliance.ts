export type BillingCycle = "monthly" | "annual";
export type PaymentMethodType = "card" | "blik" | "blik_recurring" | "fast_bank_transfer" | "traditional_bank_transfer" | "apple_pay" | "google_pay";
export interface SubscriptionConsentSnapshot { readonly termsVersion: string; readonly privacyVersion: string; readonly priceId: string; readonly cycle: BillingCycle; readonly totalAmount: string; readonly currency: "PLN" | "EUR"; readonly autoRenewal: boolean; readonly acceptedAt: string; readonly actorId: string; readonly ipHash: string; }
export interface KsefInvoiceReference { readonly localInvoiceId: string; readonly ksefNumber: string | null; readonly status: "draft" | "ready_for_ksef" | "submitted" | "accepted" | "rejected" | "offline_pending" | "correction_required"; readonly schemaVersion: string; readonly submittedAt: string | null; readonly acceptedAt: string | null; readonly upoReference: string | null; }
export interface PaymentProvider { createCheckout(input: unknown): Promise<unknown>; createMandate(input: unknown): Promise<unknown>; charge(input: unknown): Promise<unknown>; refund(input: unknown): Promise<unknown>; cancelMandate(input: unknown): Promise<unknown>; verifyWebhook(input: unknown): Promise<boolean>; reconcile(input: unknown): Promise<unknown>; }
