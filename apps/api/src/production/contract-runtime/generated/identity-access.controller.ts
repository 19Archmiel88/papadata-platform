// Generated by tools/generate-backend-contract-runtime.mjs. Do not edit manually.
import { Controller, Get, Headers, Inject, Post, Put, Req } from "@nestjs/common";
import type { FastifyRequest } from "fastify";
import { AuditDeniedAccess, OperationId, PublicEndpoint, RequireAuthLevel, RequireCapabilities } from "../../auth/route-policy.js";
import { Principal } from "../../auth/principal.decorator.js";
import type { RequestPrincipal } from "../../auth/request-principal.js";
import { ContractRuntimeService } from "../contract-runtime.service.js";
import { contractRequest } from "../contract-runtime.request.js";

@Controller()
export class IdentityAccessContractController {
  constructor(
    @Inject(ContractRuntimeService) private readonly runtime: ContractRuntimeService,
  ) {}

  @Post("v1/access/bootstrap")
  @OperationId("access.bootstrap")
  @RequireCapabilities("workspace.manage")
  @RequireAuthLevel("mfa")
  @AuditDeniedAccess()
  access_bootstrap_1(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("access.bootstrap", "POST", "/v1/access/bootstrap", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/access/resolve")
  @OperationId("access.resolve")
  @RequireCapabilities("workspace.manage")
  @RequireAuthLevel("mfa")
  @AuditDeniedAccess()
  access_resolve_2(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("access.resolve", "POST", "/v1/access/resolve", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/access/tenant/select")
  @OperationId("access.tenant.select")
  @RequireCapabilities("workspace.manage")
  @RequireAuthLevel("mfa")
  @AuditDeniedAccess()
  access_tenant_select_3(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("access.tenant.select", "POST", "/v1/access/tenant/select", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/access/tenants/list")
  @OperationId("access.tenants.list")
  @RequireCapabilities("workspace.read")
  access_tenants_list_4(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("access.tenants.list", "GET", "/v1/access/tenants/list", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/access/workspace/select")
  @OperationId("access.workspace.select")
  @RequireCapabilities("workspace.manage")
  @RequireAuthLevel("mfa")
  @AuditDeniedAccess()
  access_workspace_select_5(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("access.workspace.select", "POST", "/v1/access/workspace/select", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/access/workspaces/list")
  @OperationId("access.workspaces.list")
  @RequireCapabilities("workspace.read")
  access_workspaces_list_6(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("access.workspaces.list", "GET", "/v1/access/workspaces/list", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/auth/access/blocked")
  @OperationId("auth.access.blocked.read")
  @PublicEndpoint()
  auth_access_blocked_read_7(
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executePublic(contractRequest("auth.access.blocked.read", "GET", "/v1/auth/access/blocked", request, correlationId, idempotencyKey), {
      correlationId: correlationId ?? null,
      ipAddress: request.ip ?? null,
    });
  }


  @Post("v1/auth/access/resolve")
  @OperationId("auth.access.resolve")
  @RequireCapabilities("auth.session.revoke")
  @RequireAuthLevel("mfa")
  @AuditDeniedAccess()
  auth_access_resolve_8(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("auth.access.resolve", "POST", "/v1/auth/access/resolve", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/auth/account/link")
  @OperationId("auth.account.link")
  @RequireCapabilities("auth.session.revoke")
  @RequireAuthLevel("mfa")
  @AuditDeniedAccess()
  auth_account_link_9(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("auth.account.link", "POST", "/v1/auth/account/link", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/auth/consents")
  @OperationId("auth.consents.accept")
  @RequireCapabilities("auth.session.revoke")
  @RequireAuthLevel("mfa")
  @AuditDeniedAccess()
  auth_consents_accept_10(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("auth.consents.accept", "POST", "/v1/auth/consents", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/auth/email/resend")
  @OperationId("auth.email.resend")
  @PublicEndpoint()
  auth_email_resend_11(
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executePublic(contractRequest("auth.email.resend", "POST", "/v1/auth/email/resend", request, correlationId, idempotencyKey), {
      correlationId: correlationId ?? null,
      ipAddress: request.ip ?? null,
    });
  }


  @Post("v1/auth/email/verify")
  @OperationId("auth.email.verify")
  @PublicEndpoint()
  auth_email_verify_12(
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executePublic(contractRequest("auth.email.verify", "POST", "/v1/auth/email/verify", request, correlationId, idempotencyKey), {
      correlationId: correlationId ?? null,
      ipAddress: request.ip ?? null,
    });
  }


  @Post("v1/auth/login")
  @OperationId("auth.login")
  @PublicEndpoint()
  auth_login_13(
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executePublic(contractRequest("auth.login", "POST", "/v1/auth/login", request, correlationId, idempotencyKey), {
      correlationId: correlationId ?? null,
      ipAddress: request.ip ?? null,
    });
  }


  @Post("v1/auth/logout")
  @OperationId("auth.logout")
  @RequireCapabilities("auth.session.revoke")
  @RequireAuthLevel("mfa")
  @AuditDeniedAccess()
  auth_logout_14(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("auth.logout", "POST", "/v1/auth/logout", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/auth/mfa/confirm")
  @OperationId("auth.mfa.confirm")
  @RequireCapabilities("auth.mfa.enroll")
  @RequireAuthLevel("step_up")
  @AuditDeniedAccess()
  auth_mfa_confirm_15(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("auth.mfa.confirm", "POST", "/v1/auth/mfa/confirm", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/auth/mfa/enroll")
  @OperationId("auth.mfa.enroll")
  @RequireCapabilities("auth.mfa.enroll")
  @RequireAuthLevel("step_up")
  @AuditDeniedAccess()
  auth_mfa_enroll_16(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("auth.mfa.enroll", "POST", "/v1/auth/mfa/enroll", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/auth/mfa/verify")
  @OperationId("auth.mfa.verify")
  @RequireCapabilities("auth.mfa.enroll")
  @RequireAuthLevel("step_up")
  @AuditDeniedAccess()
  auth_mfa_verify_17(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("auth.mfa.verify", "POST", "/v1/auth/mfa/verify", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/auth/oauth/callback")
  @OperationId("auth.oauth.callback")
  @PublicEndpoint()
  auth_oauth_callback_18(
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executePublic(contractRequest("auth.oauth.callback", "POST", "/v1/auth/oauth/callback", request, correlationId, idempotencyKey), {
      correlationId: correlationId ?? null,
      ipAddress: request.ip ?? null,
    });
  }


  @Post("v1/auth/oauth/start")
  @OperationId("auth.oauth.start")
  @PublicEndpoint()
  auth_oauth_start_19(
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executePublic(contractRequest("auth.oauth.start", "POST", "/v1/auth/oauth/start", request, correlationId, idempotencyKey), {
      correlationId: correlationId ?? null,
      ipAddress: request.ip ?? null,
    });
  }


  @Post("v1/auth/password/recovery/request")
  @OperationId("auth.password.recovery.request")
  @PublicEndpoint()
  auth_password_recovery_request_20(
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executePublic(contractRequest("auth.password.recovery.request", "POST", "/v1/auth/password/recovery/request", request, correlationId, idempotencyKey), {
      correlationId: correlationId ?? null,
      ipAddress: request.ip ?? null,
    });
  }


  @Post("v1/auth/password/recovery/token/validate")
  @OperationId("auth.password.recovery.token.validate")
  @PublicEndpoint()
  auth_password_recovery_token_validate_21(
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executePublic(contractRequest("auth.password.recovery.token.validate", "POST", "/v1/auth/password/recovery/token/validate", request, correlationId, idempotencyKey), {
      correlationId: correlationId ?? null,
      ipAddress: request.ip ?? null,
    });
  }


  @Post("v1/auth/password/reset")
  @OperationId("auth.password.reset")
  @PublicEndpoint()
  auth_password_reset_22(
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executePublic(contractRequest("auth.password.reset", "POST", "/v1/auth/password/reset", request, correlationId, idempotencyKey), {
      correlationId: correlationId ?? null,
      ipAddress: request.ip ?? null,
    });
  }


  @Post("v1/auth/reauthenticate")
  @OperationId("auth.reauthenticate")
  @RequireCapabilities("auth.session.revoke")
  @RequireAuthLevel("mfa")
  @AuditDeniedAccess()
  auth_reauthenticate_23(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("auth.reauthenticate", "POST", "/v1/auth/reauthenticate", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/auth/register/email")
  @OperationId("auth.register.email")
  @PublicEndpoint()
  auth_register_email_24(
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executePublic(contractRequest("auth.register.email", "POST", "/v1/auth/register/email", request, correlationId, idempotencyKey), {
      correlationId: correlationId ?? null,
      ipAddress: request.ip ?? null,
    });
  }


  @Post("v1/auth/registration/finalize")
  @OperationId("auth.registration.finalize")
  @PublicEndpoint()
  auth_registration_finalize_25(
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executePublic(contractRequest("auth.registration.finalize", "POST", "/v1/auth/registration/finalize", request, correlationId, idempotencyKey), {
      correlationId: correlationId ?? null,
      ipAddress: request.ip ?? null,
    });
  }


  @Get("v1/auth/session")
  @OperationId("auth.session.read")
  @RequireCapabilities("auth.session.read")
  auth_session_read_26(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("auth.session.read", "GET", "/v1/auth/session", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/auth/status")
  @OperationId("auth.status.read")
  @PublicEndpoint()
  auth_status_read_27(
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executePublic(contractRequest("auth.status.read", "GET", "/v1/auth/status", request, correlationId, idempotencyKey), {
      correlationId: correlationId ?? null,
      ipAddress: request.ip ?? null,
    });
  }


  @Put("v1/company/draft")
  @OperationId("company.draft.update")
  @RequireCapabilities("workspace.manage")
  @RequireAuthLevel("mfa")
  @AuditDeniedAccess()
  company_draft_update_28(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("company.draft.update", "PUT", "/v1/company/draft", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/company/lookup")
  @OperationId("company.lookup")
  @PublicEndpoint()
  company_lookup_29(
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executePublic(contractRequest("company.lookup", "GET", "/v1/company/lookup", request, correlationId, idempotencyKey), {
      correlationId: correlationId ?? null,
      ipAddress: request.ip ?? null,
    });
  }


  @Post("v1/auth/invitations/accept")
  @OperationId("invitation.accept")
  @PublicEndpoint()
  invitation_accept_30(
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executePublic(contractRequest("invitation.accept", "POST", "/v1/auth/invitations/accept", request, correlationId, idempotencyKey), {
      correlationId: correlationId ?? null,
      ipAddress: request.ip ?? null,
    });
  }


  @Get("v1/invitation/read")
  @OperationId("invitation.read")
  @RequireCapabilities("tenant.membership.read")
  invitation_read_31(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("invitation.read", "GET", "/v1/invitation/read", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/invitation/reject")
  @OperationId("invitation.reject")
  @RequireCapabilities("tenant.membership.manage")
  @RequireAuthLevel("step_up")
  @AuditDeniedAccess()
  invitation_reject_32(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("invitation.reject", "POST", "/v1/invitation/reject", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/auth/invitations/request")
  @OperationId("invitation.request")
  @RequireCapabilities("tenant.membership.manage")
  @RequireAuthLevel("step_up")
  @AuditDeniedAccess()
  invitation_request_33(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("invitation.request", "POST", "/v1/auth/invitations/request", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/auth/invitations/validate")
  @OperationId("invitation.validate")
  @PublicEndpoint()
  invitation_validate_34(
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executePublic(contractRequest("invitation.validate", "POST", "/v1/auth/invitations/validate", request, correlationId, idempotencyKey), {
      correlationId: correlationId ?? null,
      ipAddress: request.ip ?? null,
    });
  }


  @Post("v1/mobile/device/manage")
  @OperationId("mobile.device.manage")
  @RequireCapabilities("workspace.manage")
  @RequireAuthLevel("mfa")
  @AuditDeniedAccess()
  mobile_device_manage_35(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("mobile.device.manage", "POST", "/v1/mobile/device/manage", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/mobile/invite")
  @OperationId("mobile.invite")
  @RequireCapabilities("workspace.manage")
  @RequireAuthLevel("mfa")
  @AuditDeniedAccess()
  mobile_invite_36(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("mobile.invite", "POST", "/v1/mobile/invite", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/mobile/use")
  @OperationId("mobile.use")
  @RequireCapabilities("workspace.read")
  mobile_use_37(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("mobile.use", "GET", "/v1/mobile/use", request, correlationId, idempotencyKey),
    );
  }


  @Put("v1/onboarding/profile")
  @OperationId("onboarding.profile.update")
  @RequireCapabilities("workspace.manage")
  @RequireAuthLevel("mfa")
  @AuditDeniedAccess()
  onboarding_profile_update_38(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("onboarding.profile.update", "PUT", "/v1/onboarding/profile", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/onboarding/progress")
  @OperationId("onboarding.progress.read")
  @RequireCapabilities("workspace.read")
  onboarding_progress_read_39(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("onboarding.progress.read", "GET", "/v1/onboarding/progress", request, correlationId, idempotencyKey),
    );
  }


  @Post("v1/workspace/onboarding/update")
  @OperationId("workspace.onboarding.update")
  @RequireCapabilities("workspace.manage")
  @RequireAuthLevel("mfa")
  @AuditDeniedAccess()
  workspace_onboarding_update_40(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("workspace.onboarding.update", "POST", "/v1/workspace/onboarding/update", request, correlationId, idempotencyKey),
    );
  }


  @Get("v1/workspace/resolve")
  @OperationId("workspace.resolve")
  @RequireCapabilities("workspace.read")
  workspace_resolve_41(
    @Principal() principal: RequestPrincipal,
    @Req() request: FastifyRequest,
    @Headers("x-correlation-id") correlationId?: string,
    @Headers("idempotency-key") idempotencyKey?: string,
  ): Promise<object> {
    return this.runtime.executeAuthenticated(
      principal,
      contractRequest("workspace.resolve", "GET", "/v1/workspace/resolve", request, correlationId, idempotencyKey),
    );
  }
}
