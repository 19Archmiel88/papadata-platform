export type ComponentId = string;
export type TestId = string;
export type Locale = 'pl' | 'en';
export type Density = 'comfortable' | 'compact';
export type ReadinessState = 'ready' | 'partial' | 'stale' | 'processing' | 'noData' | 'sourceError' | 'blocked';
export type ComponentState = 'default' | 'loading' | 'disabled' | 'error' | 'empty' | 'readonly' | 'success';
export type ComponentVariant = 'default' | 'subtle' | 'strong' | 'critical' | 'success' | 'warning' | 'info';
export type DatePreset = 'today' | 'yesterday' | 'last7d' | 'last30d' | 'monthToDate' | 'custom';
export type SearchScope = 'global' | 'module' | 'workspace' | 'help';
export type SearchSuggestion = { id: string; label: string; scope: SearchScope; href?: string };
export type DateRange = { from: string; to: string; timezone: string; preset?: DatePreset };
export type WorkspaceContext = { tenantId: string; workspaceId: string; locale: Locale; range?: DateRange; userId?: string };
export type DataSourceRef = { provider: string; dataset: string; lastSyncAt: string; completeness: number };
export type EvidenceRef = { id: string; label: string; source: string; href?: string; confidence?: number };
export type DomainActionEvent = { componentId: ComponentId; action: string; itemId?: string; screenId?: string; correlationId?: string };
export type DomainItem = { id: string; label: string; value?: string | number | boolean | null; status?: ReadinessState; evidenceIds?: string[] };
export type ChartPoint = { x: string; y: number | null; label?: string; confidenceLow?: number; confidenceHigh?: number };
export type ChartSeries = { id: string; label: string; points: ChartPoint[]; unit?: string };
export type DataColumn = { id: string; label: string; align?: 'left' | 'right' | 'center'; sortable?: boolean; width?: number };
export type DataRow = Record<string, string | number | boolean | null> & { id?: string };
export type SortState = { columnId: string; direction: 'asc' | 'desc' } | null;
export type PaginationState = { page: number; pageSize: number; totalItems: number };
export type SelectionState = { selectedIds: string[]; mode: 'none' | 'single' | 'multiple' };
export type TableModel = { columns: DataColumn[]; rows: DataRow[]; sort?: SortState; pagination?: PaginationState; selection?: SelectionState };
export type ButtonVariant = 'primary' | 'secondary' | 'ghost' | 'danger' | 'link';
export type ButtonSize = 'sm' | 'md' | 'lg';
export type IntegrationProvider = 'shopify' | 'allegro' | 'google' | 'meta' | 'csv' | 'api';
export type PairingStepStatus = 'notStarted' | 'active' | 'waitingForProvider' | 'verified' | 'failed' | 'expired';
export type DevicePairingStatus = 'unpaired' | 'pending' | 'paired' | 'revoked';
export type BudgetPacingStatus = 'underPace' | 'onPace' | 'overPace' | 'risk';
export type DecisionStatus = 'new' | 'review' | 'approved' | 'rejected' | 'measured';
export type SyncStatus = 'queued' | 'running' | 'partial' | 'completed' | 'failed';
